# EduPulse AI — Agent Skills

Load these before editing the platform code:

| Skill | When |
|-------|------|
| `.agents/skills/edupulse-react-native-ui/` | Any `edupulse/frontend/` work |
| `.agents/skills/react-best-practices/` | React performance (client/rerender rules) |
| `.agents/skills/ui-ux-pro-max/` | UI design tokens; use `--stack react-native` |
| `.agents/skills/edupulse-nestjs-api/` | Any `edupulse/backend/` work |

BMad workflows live in `.agents/skills/bmad-*/`.
